#include <string.h>
#include "main.h"

/**
 * *_strcmp - function compare string =s
 * and return 0 if equal or 1 if not
 * @s1: a pointer to a char
 * @:s2 a pointer to a char
 * Return: return value of dest
 */

int _strcmp(char *s1, char *s2)
{
	return(strcmp(*s1, *s2);
}
