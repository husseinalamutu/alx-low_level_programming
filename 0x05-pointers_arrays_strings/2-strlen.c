#include <string.h>
#include "main.h"

/**
 * Description - a simple strlen function
 * _strlen: a function that returns the length of a string
 * @s: length of string to be returned
 * Return: returns string lenth
 */

int _strlen(char *s)
{
	return (strlen(*s));
}

